Install npm dependencies for FinTrack.

Run `npm install --cache ~/.npm-fintrack-cache` in the project root. The custom cache flag avoids permission issues with the default npm cache. After install, run `npx expo install --fix` to align versions with the current Expo SDK.
