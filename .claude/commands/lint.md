Run ESLint on the FinTrack codebase and report issues.

Run `npx eslint . --ext .ts,.tsx` in the project root. If the user asks to fix, run with `--fix` flag. Report any remaining issues that need manual fixing.
