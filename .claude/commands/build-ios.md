Test the iOS production bundle build.

Run `npx expo export --platform ios` in the project root. This verifies the app bundles correctly for iOS without deploying. Report success or any bundling errors. Clean up the `dist/` folder after verification.
