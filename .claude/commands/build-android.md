Test the Android production bundle build.

Run `npx expo export --platform android` in the project root. This verifies the app bundles correctly for Android without deploying. Report success or any bundling errors. Clean up the `dist/` folder after verification.
