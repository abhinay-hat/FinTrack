Create a new reusable component for FinTrack.

Ask the user for the component name and purpose, then:
1. Create the component file at `src/components/{ComponentName}.tsx`
2. Use TypeScript with proper prop types
3. Use NativeWind className for styling
4. Follow the design system tokens (colors, typography, spacing, radius)
5. Use Phosphor icons if icons are needed (Duotone weight for categories, Regular/Fill for UI)
6. Ensure minimum 44x44dp touch targets for interactive elements
